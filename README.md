# SKELETON FOR BUILD HTML #

This repository contains pages and assets.

### How do I get set up? ###

Use ``npm install``

### How to re-build CSS file? ###

First, install gulp globally ``npm install -g gulp`` then ``gulp build-css``